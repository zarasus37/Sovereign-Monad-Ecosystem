# LOGOC_MODEL_v5.json — Canonical Location

The canonical release copy of `LOGOC_MODEL_v5.json` lives in:

**`theo-techno-cosmo/plex/Manifest/LOGOC_MODEL_v5.json`**

A working-session variant with additional `split_handling` and `inference_schema` annotations is preserved in the same directory as:

**`theo-techno-cosmo/plex/Review/LOGOC_MODEL_v5_REVIEW_VARIANT.json`**

The Review directory is for interpretation and audit material. Raw model artifacts should be referenced from `plex/Manifest/` unless you are specifically studying the review variant.
