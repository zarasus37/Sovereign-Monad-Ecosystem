# logoc_inference_v5.py — Canonical Location

The canonical reference copy of `logoc_inference_v5.py` lives in:

**`theo-techno-cosmo/plex/CODE/logoc_inference_reference.py`**

The original release copy remains in:

**`theo-techno-cosmo/plex/Manifest/logoc_inference_v5.py`**

A working-session variant with additional `tier_locked` / `tier_split` fields is preserved in the same directory as:

**`theo-techno-cosmo/plex/Review/logoc_inference_v5_REVIEW_VARIANT.py`**

The active production inference pipeline is in:

- `monad-ecosystem/packages/logoc/peirce/pipeline.py`
- `gnostic-engine/src/gnostic_engine/core/logoc_pipeline.py`

The Review directory is for interpretation and audit material. Executable reference artifacts should be referenced from `plex/CODE/` or the production packages.
