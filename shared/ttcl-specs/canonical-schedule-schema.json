{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://the-sovereign/ttcl-specs/canonical-schedule-schema.json",
  "title": "TTCL Canonical Schedule — Layer 6 Scheduler output contract",
  "description": "The `canonical_schedule.json` artifact produced by the @sovereign/scheduler Layer 6 optimizer — the default rotation sequence for data generation (the prose's 'canonical_schedule.json that tells you the default rotation sequence for data generation', TTCL_v1_0_BREAKDOWN.md:271). Records the annealing config, the wheel/pair registry snapshot the run was generated against, the initial state, every step (state + objective terms + temperature + move + accepted flag + ΔJ), the best state by J, and coverage statistics (distinct composites visited, accepted-step count, per-domain visit balance). Validated by the facade at generation time with ajv. Default artifact: shared/fixtures/layer6/canonical-schedule.json. The artifact is reproducible byte-for-byte from a fixed seed (the spec's 'run multiple seeds' mitigation requires seedability).",
  "type": "object",
  "required": ["version", "generator", "config", "registry", "initial", "steps", "best", "coverage"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string", "description": "Optional JSON Schema reference (the schema's own $id)." },
    "version": { "type": "string", "minLength": 1, "description": "Artifact version (e.g. '1.0.0')." },
    "generator": { "type": "string", "minLength": 1, "description": "Generator identifier (e.g. 'ttcl-scheduler-v1')." },
    "config": {
      "type": "object",
      "required": ["weights", "T_init", "T_min", "cooling", "steps", "seed", "windowN"],
      "additionalProperties": false,
      "properties": {
        "weights": {
          "type": "object",
          "description": "Objective weights — six terms: α (Coverage), β (Locality), γ (Tripartite), δ (Cost, negated), ε (Letter-pair validity), ζ (Fourth-Figure Tabula Generalis validity). Spec: J = αC + βL + γT − δS + εP + ζF.",
          "required": ["alpha", "beta", "gamma", "delta", "epsilon", "zeta"],
          "additionalProperties": false,
          "properties": {
            "alpha": { "type": "number", "description": "Coverage weight (default 0.35)." },
            "beta":  { "type": "number", "description": "Locality weight (default 0.25)." },
            "gamma": { "type": "number", "description": "Tripartite weight (default 0.30)." },
            "delta": { "type": "number", "description": "Cost weight, subtracted (default 0.10)." },
            "epsilon": { "type": "number", "description": "Letter-pair validity weight — εP term (default 0.05)." },
            "zeta":    { "type": "number", "description": "Fourth-Figure Tabula Generalis validity weight — ζF term (default 0.02)." }
          }
        },
        "T_init": { "type": "number" },
        "T_min": { "type": "number" },
        "cooling": { "type": "number" },
        "steps": { "type": "integer", "minimum": 1 },
        "seed": { "type": "integer" },
        "windowN": { "type": "integer", "minimum": 1 }
      }
    },
    "registry": {
      "type": "object",
      "required": ["registry", "wheels", "facets", "pairs"],
      "additionalProperties": false,
      "properties": {
        "registry": { "type": "string" },
        "wheels": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name", "size", "domains"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "size": { "type": "integer", "minimum": 1 },
              "domains": { "type": "array", "items": { "enum": ["THEOLOGY", "TECHNOLOGY", "COSMOLOGY"] }, "uniqueItems": true }
            }
          }
        },
        "facets": {
          "type": "object",
          "required": ["THEOLOGY", "TECHNOLOGY", "COSMOLOGY"],
          "additionalProperties": false,
          "properties": {
            "THEOLOGY": { "type": "string" },
            "TECHNOLOGY": { "type": "string" },
            "COSMOLOGY": { "type": "string" }
          }
        },
        "pairs": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "wheels"],
            "additionalProperties": false,
            "properties": {
              "id": { "type": "string" },
              "wheels": { "type": "array", "minItems": 2, "maxItems": 2, "uniqueItems": true, "items": { "type": "string" } }
            }
          }
        }
      }
    },
    "initial": { "$ref": "#/definitions/state" },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["t", "state", "terms", "temperature", "move", "accepted", "deltaJ"],
        "additionalProperties": false,
        "properties": {
          "t": { "type": "integer", "minimum": 0 },
          "state": { "$ref": "#/definitions/state" },
          "terms": {
            "type": "object",
            "description": "Objective terms for this step. Seven values: C (Coverage), L (Locality), T (Tripartite), S (Cost), P (Letter-pair validity), F (Fourth-Figure validity), J (scalar objective). Spec: J = αC + βL + γT − δS + εP + ζF.",
            "required": ["C", "L", "T", "S", "P", "F", "J"],
            "additionalProperties": false,
            "properties": {
              "C": { "type": "number", "description": "Coverage — 1 if this step's composite is newly visited, else 0." },
              "L": { "type": "number", "description": "Locality — 1 − (roughness − 1)/(MAX_ROUGHNESS − 1); smoother moves score higher. Range [0, 1]." },
              "T": { "type": "number", "description": "Tripartite — fraction of the 3 TTCL domains present in the trailing N-step window. Range [0, 1]." },
              "S": { "type": "number", "description": "Cost — composites materialized, normalized to [0, 1]." },
              "P": { "type": "number", "description": "Letter-pair validity (εP term) — 1 if the current pair is a valid 45-pair combination, 0 otherwise, 1 if letter-pairs are not configured." },
              "F": { "type": "number", "description": "Fourth-Figure Tabula Generalis validity (ζF term) — 1 if the 3-wheel composite is a valid 84-camera combination, 0 otherwise, 1 if the Fourth Figure is not enabled." },
              "J": { "type": "number", "description": "Scalar objective — J = αC + βL + γT − δS + εP + ζF." }
            }
          },
          "temperature": { "type": "number" },
          "move": { "enum": ["initial", "LocalRotate", "PatternSwitch", "WheelSwap"] },
          "accepted": { "type": "boolean" },
          "deltaJ": { "type": "number" }
        }
      }
    },
    "best": {
      "type": "object",
      "required": ["step", "state", "J"],
      "additionalProperties": false,
      "properties": {
        "step": { "type": "integer", "minimum": 0 },
        "state": { "$ref": "#/definitions/state" },
        "J": { "type": "number" }
      }
    },
    "coverage": {
      "type": "object",
      "required": ["distinctComposites", "totalSteps", "acceptedSteps", "domainVisits"],
      "additionalProperties": false,
      "properties": {
        "distinctComposites": { "type": "integer", "minimum": 1 },
        "totalSteps": { "type": "integer", "minimum": 1 },
        "acceptedSteps": { "type": "integer", "minimum": 0 },
        "domainVisits": {
          "type": "object",
          "required": ["THEOLOGY", "TECHNOLOGY", "COSMOLOGY"],
          "additionalProperties": false,
          "properties": {
            "THEOLOGY": { "type": "integer", "minimum": 0 },
            "TECHNOLOGY": { "type": "integer", "minimum": 0 },
            "COSMOLOGY": { "type": "integer", "minimum": 0 }
          }
        }
      }
    }
  },
  "definitions": {
    "state": {
      "type": "object",
      "required": ["offsets", "pattern", "facets"],
      "additionalProperties": false,
      "properties": {
        "offsets": {
          "type": "object",
          "description": "Wheel name → offset in [0, size).",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        },
        "pattern": { "type": "string", "minLength": 1 },
        "facets": {
          "type": "object",
          "required": ["THEOLOGY", "TECHNOLOGY", "COSMOLOGY"],
          "additionalProperties": false,
          "properties": {
            "THEOLOGY": { "type": "string" },
            "TECHNOLOGY": { "type": "string" },
            "COSMOLOGY": { "type": "string" }
          }
        }
      }
    }
  }
}