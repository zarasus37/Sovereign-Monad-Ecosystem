{
  "project": "The Sovereign",
  "canonical_root": "C:\\Users\\crisc\\OneDrive - Southern Careers Institute\\My Drive\\The_Sovereign",
  "resume_order": [
    "README.md",
    "docs/SOVEREIGN_MONAD_ECOSYSTEM_MASTER_OPERATING_FILE_v2.4.0.md",
    "docs/PROJECT_STATE.md",
    "docs/REPO_STRUCTURE_MAP.md"
  ],
  "active_pillars": [
    "theo-techno-cosmo",
    "gnostic-engine",
    "monad-ecosystem"
  ],
  "archive_root": "archive",
  "current_status": {
    "structure": "clean",
    "build": "passing",
    "test": "passing",
    "lint": "passing",
    "layout_check": "passing; legacy-path scan skipped with warning when ripgrep is unavailable",
    "frontend_typecheck": "passing",
    "frontend_build": "passing"
  },
  "known_follow_ups": [
    "x402-bridge sovereignty remediation: cost-accounting ledger, documented failure/retry envelope (fix dead X402_MAX_CONCURRENT knob + User-Agent gap), and wiring to a sovereign-agent consumer (or archive). Smoke test GREEN 2026-07-10 (PR #30, eth_blockNumber on monad-mainnet); package remains LEGACY_NON_SOVEREIGN per docs/LEGACY_COMPONENTS.md §6.",
    "Layer 7 data-generation consumer LANDED (this PR): new @sovereign/gnosis-training-data package turns a Layer 6 canonical_schedule into Gnosis training events (JSONL) by reusing @sovereign/ttcl scoreSign — deterministic + byte-reproducible, one event per accepted step, Catalan slot labels emitted as null pending the data asset. Still unbuilt: the full Layer 7 Training Pipeline (SFT→Reward→PPO→Eval; LLaMA 3.1 8B + QLoRA + HuggingFace TRL) — a real GPU job, deliberately not stubbed. Still open as DATA (not code): the real 45-pair Llull PairTable + Catalan slot labels + the Theologia wheel (the scheduler + consumer are data-driven via shared/fixtures/layer6/wheel-registry.json, so they drop in as a data change). Out of spec scope: a scheduler CLI / W&B tracking.",
    "Layer 7 training-pipeline wiring LANDED (this PR): new root-level gnosis-training/ Python package (uv-managed, sibling to gnostic-engine/) — real SFTTrainer/RewardTrainer/GRPOTrainer + QLoRA configs as runnable code (TTCL_v1_0_BREAKDOWN.md:275-311), import-smoke-verified, CPU-verified. NO training executed — the LLaMA 3.1 8B run is an explicitly-future GPU job (deliberately not stubbed, no fake completion). Every spec silence concretized in generated/hyperparams.py (LoRA r=64/α=128/dropout=0.05, AdamW lr=2e-4 cosine warmup 3%, 4-bit nf4, GRPO β=0.04/ε=0.2/num_generations=8, seed=42, eval thresholds). STAGE-3 OPTIMIZER = GRPO (doctrinal upgrade from PPO; see the NOTE at TTCL_v1_0_BREAKDOWN.md:302) — GRPO is the PPO-family optimizer that drops the learned value model for a group-relative baseline (the DeepSeek-R1/Qwen optimizer). TRL VERSION (modern, no upper cap): trl>=0.15 — TRL removed the PPO stack (PPOConfig/PPOTrainer) after the 0.12-0.13 PPOv2 window (gone by 0.14+ and all 1.x, where SFT/Reward/GRPO/DPO/KTO remain); grpo.py targets the modern GRPOTrainer/GRPOConfig constructor signature (reward_funcs = the Stage-2 reward model; no value model), so the package requires modern TRL with no upper cap (uv sync resolves trl==1.8.0) — GRPO is genuinely runnable, and the doctrinal upgrade is surfaced in the spec NOTE (not a silent substitution). Preference-pair builder emits a bootstrap worksheet (empty responses + rubric scores pre-filled from constitution_score) for a human authoring pass; the reward model still trains on HUMAN-judged pairs (spec line 478), not the scaffold — constitution_score is NOT the RM label / NOT a GRPO reward (seeds the worksheet rubric + gates SFT via passes). W&B/MLflow/DVC + bitsandbytes wired-but-deferred behind opt-in extras (tracking/dvc/qlora). Local-only pytest posture (NO Python added to ci.yml; mirrors gnostic-engine/); pnpm-lock.yaml untouched (uv.lock, not pnpm). Verified: ruff clean, mypy --strict 17 files clean WITH the full ML stack installed (uv sync resolves trl==1.8.0 under the modern-TRL un-pin — the honest green, not absent-deps green; 5 type: ignore[attr-defined] markers on trl re-exports because trl 1.8 ships a typed __init__.pyi that doesn't explicitly list those names — imports resolve at runtime), pytest 67 passed/0 skipped with the stack / 61 passed/2 skipped without (datasets + smoke-imports skip via importorskip — honest), pnpm check:layout green for gnosis-training (only the pre-existing untracked Shaliah Agents/ flagged — not this PR's). Remaining frontier: the full Layer 7 GPU run (SFT→Reward→GRPO→Eval) + human-judged preference pairs + the 45-pair PairTable/Catalan labels/Theologia wheel data asset."
  ],
  "last_validated": "2026-07-13"
}
