import StubPage from "./StubPage";
export default function SynapsePage() {
  return (
    <StubPage
      layer={4}
      title="Signal Routing (Synapse)"
      description="Neural signal routing layer — event bus, message queuing, and inter-agent communication channels."
    />
  );
}
