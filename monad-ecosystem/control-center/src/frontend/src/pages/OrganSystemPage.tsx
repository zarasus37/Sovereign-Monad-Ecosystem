import StubPage from "./StubPage";
export default function OrganSystemPage() {
  return (
    <StubPage
      layer={3}
      title="Organ System"
      description="Specialized organ modules — each responsible for a distinct functional domain within the ecosystem."
    />
  );
}
