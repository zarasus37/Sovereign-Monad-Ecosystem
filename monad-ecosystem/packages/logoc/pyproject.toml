[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "logoc"
version = "1.0.0"
description = "LOGOC (Logico-Ontological Gnostic Operations Corpus) Python implementation"
authors = ["Sovereign Team"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.0.0"
pyyaml = "^6.0.1"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
