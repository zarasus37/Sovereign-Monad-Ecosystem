# Gnosis Core Plurality Scheduler — Docker Compose deployment
#
# Usage:
#   docker compose -f monad-ecosystem/packages/gnosis-core/docker-compose.yml up -d
#
# Override environment variables in a .env file next to this compose file or
# pass them on the command line:
#
#   PLURALITY_PROVIDER=registry \
#     AGENT_REGISTRY_URL=http://agent-registry:8080/agents \
#     docker compose -f monad-ecosystem/packages/gnosis-core/docker-compose.yml up -d

services:
  agent-registry:
    build:
      context: ../../..
      dockerfile: monad-ecosystem/packages/gnosis-core/Dockerfile.registry
    image: sovereign-agent-registry:latest
    container_name: sovereign-agent-registry
    restart: unless-stopped
    environment:
      REGISTRY_PORT: 8080
      REGISTRY_DATA_PATH: /data/agents.json
      REGISTRY_AUTH_TOKEN: ${REGISTRY_AUTH_TOKEN:-}
    volumes:
      - ${AGENT_PROFILES_HOST_PATH:-./data}:/data:ro
    ports:
      - ${REGISTRY_HOST_PORT:-8080}:8080
    healthcheck:
      test: ['CMD', 'node', '-e', "require('http').get('http://localhost:8080/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  gnosis-core:
    build:
      context: ../../..
      dockerfile: monad-ecosystem/packages/gnosis-core/Dockerfile
    image: sovereign-gnosis-core:latest
    container_name: sovereign-gnosis-core
    restart: unless-stopped
    depends_on:
      - agent-registry
    environment:
      # Scheduler cadence and thresholds
      PLURALITY_INTERVAL_MS: ${PLURALITY_INTERVAL_MS:-900000}
      PLURALITY_THRESHOLD: ${PLURALITY_THRESHOLD:-0.6}
      PLURALITY_SOURCE: ${PLURALITY_SOURCE:-gnosis-core-plurality}
      PLURALITY_HEALTH_PORT: ${PLURALITY_HEALTH_PORT:-8080}
      PLURALITY_STATE_PATH: ${PLURALITY_STATE_PATH:-/app/monad-ecosystem/packages/gnosis-core/state/plurality-state.json}

      # Provider selection: file | registry
      PLURALITY_PROVIDER: ${PLURALITY_PROVIDER:-file}

      # File provider configuration
      AGENT_PROFILES_PATH: ${AGENT_PROFILES_PATH:-/data/classified-agents.json}

      # Registry provider configuration
      # Default points at the in-compose agent-registry service.
      AGENT_REGISTRY_URL: ${AGENT_REGISTRY_URL:-http://agent-registry:8080/agents}
      AGENT_REGISTRY_TOKEN: ${AGENT_REGISTRY_TOKEN:-}
      AGENT_REGISTRY_TIMEOUT_MS: ${AGENT_REGISTRY_TIMEOUT_MS:-10000}

      # Optional Kafka bridge activation
      KAFKA_ENABLED: ${KAFKA_ENABLED:-false}
      KAFKA_BROKERS: ${KAFKA_BROKERS:-}
      KAFKA_CLIENT_ID: ${KAFKA_CLIENT_ID:-sovereign-gnosis-core}
    volumes:
      # Mount agent profiles when using the file provider.
      # Unused when PLURALITY_PROVIDER=registry.
      - ${AGENT_PROFILES_HOST_PATH:-./data}:/data:ro
      # Persist emitted signal stream on the host for easy inspection.
      - ${SIGNAL_LOG_HOST_PATH:-./logs}:/app/monad-ecosystem/packages/gnosis-core/logs
      # Persist rising-edge state across container restarts.
      - ${PLURALITY_STATE_HOST_PATH:-./state}:/app/monad-ecosystem/packages/gnosis-core/state
    expose:
      - ${PLURALITY_HEALTH_PORT:-8080}
    healthcheck:
      test: ['CMD', 'node', '-e', "require('http').get('http://localhost:${PLURALITY_HEALTH_PORT:-8080}/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
